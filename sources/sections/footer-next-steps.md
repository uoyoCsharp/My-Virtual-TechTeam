## Suggested Next Steps

Based on the skill just completed and the current project state, recommend 2-3 relevant next skills. Consider:

1. The skill just completed: `{{current_skill}}`
2. The current `skill_history` in session.yaml
3. All available skills listed in registry.yaml (including custom skills)
4. The `category` and `description` of each skill for relevance matching
5. The current `active_change` state (if a change is in progress)

Format each suggestion as:
- `/{skill_name}` -- {when to use this skill, tailored to the current context}

Do not suggest the skill that was just completed. Prioritize skills that logically follow from the work done.
